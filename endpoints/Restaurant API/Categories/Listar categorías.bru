meta {
  name: Listar categorías
  type: http
  seq: 1
}

get {
  url: {{host}}/api/categories
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "{{name}}",
    "description": {{description}}
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
