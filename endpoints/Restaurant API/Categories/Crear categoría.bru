meta {
  name: Crear categoría
  type: http
  seq: 3
}

post {
  url: {{host}}/api/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "{{name}}",
    "description": "{{description}}"
  }
}

vars:pre-request {
  name: Postres
  description: Deliciosos postres
}

settings {
  encodeUrl: true
  timeout: 0
}
