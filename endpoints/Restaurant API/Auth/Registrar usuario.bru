meta {
  name: Registrar usuario
  type: http
  seq: 1
}

post {
  url: {{host}}/api/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{name}}",
    "email": "{{email}}",
    "password": "{{password}}"
  }
}

vars:pre-request {
  name: Brian Batz
  email: brian@gmail.com
  password: Bbbbbbb7
}

settings {
  encodeUrl: true
  timeout: 0
}
