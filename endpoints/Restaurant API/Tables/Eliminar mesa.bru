meta {
  name: Eliminar mesa
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/tables/{{id}}
  body: none
  auth: inherit
}

vars:pre-request {
  id: 
}

settings {
  encodeUrl: true
  timeout: 0
}
