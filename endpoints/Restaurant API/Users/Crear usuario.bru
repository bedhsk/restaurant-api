meta {
  name: Crear usuario
  type: http
  seq: 3
}

post {
  url: {{host}}/api/users
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{name}}",
    "email": "{{email}}",
    "password": "{{password}}",
    "role": "{{role}}"
  }
}

vars:pre-request {
  name: Brian Batz
  email: brianbatz@example.com
  password: kycgato123
  role: admin
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  
  Roles
  - admin
  - manager
  - waiter
  - cashier
}
